{
  "name": "scut-register",
  "version": "0.1.0",
  "description": "SCUT registration service. Hosted onboarding endpoint that mints SII identities on OpenSCUTRegistry on behalf of agents.",
  "license": "MIT",
  "author": "Doug Hardman <doug@mrdoug.com>",
  "homepage": "https://openscut.ai",
  "repository": {
    "type": "git",
    "url": "https://github.com/douglashardman/openscut.git",
    "directory": "packages/register"
  },
  "type": "module",
  "bin": {
    "scut-register": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --no-error-on-unmatched-pattern",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "spike:mint": "node --env-file=../../.env.local --import tsx scripts/spike-mint.ts"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.3.0",
    "@openscut/core": "workspace:*",
    "better-sqlite3": "^12.9.0",
    "fastify": "^5.8.5",
    "libsodium-wrappers": "^0.8.4",
    "viem": "^2.48.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/libsodium-wrappers": "^0.8.2"
  },
  "engines": {
    "node": ">=22"
  }
}
