{
  "name": "scut-relay",
  "version": "0.1.0",
  "description": "SCUT relay daemon. Store-and-forward for encrypted envelopes.",
  "license": "MIT",
  "author": "Doug Hardman <doug@mrdoug.com>",
  "homepage": "https://openscut.ai",
  "repository": {
    "type": "git",
    "url": "https://github.com/douglashardman/openscut.git",
    "directory": "packages/relay"
  },
  "type": "module",
  "bin": {
    "scut-relay": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src test --no-error-on-unmatched-pattern",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.3.0",
    "@openscut/core": "workspace:*",
    "better-sqlite3": "^12.9.0",
    "fastify": "^5.8.5",
    "libsodium-wrappers": "^0.8.4",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/libsodium-wrappers": "^0.8.2"
  }
}
